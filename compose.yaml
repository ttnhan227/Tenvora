services:
  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: VeriSpend
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d VeriSpend"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      PORT: 8080
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: Host=database;Port=5432;Database=VeriSpend;Username=postgres;Password=postgres
      JwtSettings__Secret: local-development-jwt-secret-change-before-production
      CLIENT_ORIGINS: http://localhost:5173
      APP_BASE_URL: http://localhost:5173
      AiProvider__Name: ${AI_PROVIDER_NAME:-Mistral}
      AiProvider__ApiKey: ${AI_PROVIDER_API_KEY:-}
      AiProvider__Endpoint: ${AI_PROVIDER_ENDPOINT:-https://api.mistral.ai/v1/chat/completions}
      AiProvider__ChatModel: ${AI_CHAT_MODEL:-mistral-small-latest}
      AiProvider__VisionModel: ${AI_VISION_MODEL:-mistral-small-latest}
      MistralSettings__ApiKey: ${MISTRAL_API_KEY:-}
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy

  frontend:
    image: node:20-alpine
    working_dir: /app
    environment:
      VITE_API_BASE_URL: http://localhost:8080/api
    command: sh -c "test -x node_modules/.bin/vite || npm ci; npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - client_node_modules:/app/node_modules
    depends_on:
      - backend

volumes:
  postgres_data:
  client_node_modules:
